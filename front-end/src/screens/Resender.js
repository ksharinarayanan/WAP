import { Typography } from "@material-ui/core";
import React from "react";

function Proxy(props) {
    return (
        <div>
            <Typography>Resender!</Typography>
        </div>
    );
}

export default Proxy;
