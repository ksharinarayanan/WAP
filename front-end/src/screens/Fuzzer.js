import { Typography } from "@material-ui/core";
import React from "react";

function Proxy(props) {
  return (
    <div>
      <Typography>Fuzzer here!</Typography>
    </div>
  );
}

export default Proxy;
