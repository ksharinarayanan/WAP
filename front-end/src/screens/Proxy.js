import { Typography } from "@material-ui/core";
import React from "react";

function Proxy(props) {
    return (
        <div>
            <Typography>Proxy here!</Typography>
        </div>
    );
}

export default Proxy;
